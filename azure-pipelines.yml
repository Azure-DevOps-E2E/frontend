trigger:
  batch: true
  branches:
    include:
    - feature/*

pr:
  autoCancel: true
  branches:
    include:
    - dev
    - main

resources:
  repositories:
  - repository: templates
    type: github
    endpoint: github.com_Azure-DevOps-E2E
    name: Azure-DevOps-E2E/config-management
    ref: refs/heads/main

variables:
- group: frontend
- name: serviceName
  value: 'frontend'
- name: nodeVersion
  value: '24.x'
- name: testResultsFile
  value: '$(System.DefaultWorkingDirectory)/reports/test-results.xml'
- name: coverageSummaryFile
  value: '$(System.DefaultWorkingDirectory)/coverage/lcov.info'

pool:
  name: 'msc Azure Pipelines'

extends:
  template: pipelines/templates/pipeline-contract.yml@templates
  parameters:
    pipelineStages:
    - stage: CI
      displayName: 'CI - $(serviceName)'
      jobs:
      - job: Quality
        displayName: 'Test and build $(serviceName)'
        steps:
        - checkout: self
          clean: true
          fetchDepth: 0
          displayName: 'Checkout source code'

        - template: steps/detect-changes.yml@templates
          parameters:
            targetBranch: 'main'

        - template: steps/security-scan.yml@templates
          parameters:
            runtime: node
            condition: and(succeeded(), eq(variables['detect.skipHeavyCI'], 'false'))

        - template: steps/container/prepare-build-image.yml@templates
          parameters:
            runtime: node
            version: '$(nodeVersion)'
            condition: and(succeeded(), eq(variables['detect.skipHeavyCI'], 'false'))

        - template: steps/node/vitest-quality-container.yml@templates
          parameters:
            cacheKey: '$(serviceName)-node$(nodeVersion)'
            testResultsFile: reports/test-results.xml
            coverageDirectory: coverage
            validationCommand: 'npm run typecheck'
            buildCommand: 'npm run build'
            condition: and(succeeded(), eq(variables['detect.skipHeavyCI'], 'false'))

        - task: PublishTestResults@2
          displayName: 'Publish test report'
          condition: and(succeededOrFailed(), eq(variables['detect.skipHeavyCI'], 'false'))
          inputs:
            testResultsFormat: JUnit
            testResultsFiles: '$(testResultsFile)'
            searchFolder: '$(System.DefaultWorkingDirectory)'
            mergeTestResults: true
            failTaskOnFailedTests: true
            failTaskOnFailureToPublishResults: true
            failTaskOnMissingResultsFile: false
            testRunTitle: '$(serviceName) tests'

        - task: UseDotNet@2
          displayName: 'Use .NET SDK for coverage publisher'
          condition: and(succeededOrFailed(), eq(variables['detect.skipHeavyCI'], 'false'))
          inputs:
            packageType: sdk
            version: '8.x'

        - task: PublishCodeCoverageResults@2
          displayName: 'Publish coverage report'
          condition: and(succeededOrFailed(), eq(variables['detect.skipHeavyCI'], 'false'))
          inputs:
            summaryFileLocation: '$(coverageSummaryFile)'
            pathToSources: '$(Build.SourcesDirectory)'
            failIfCoverageEmpty: true

        - template: steps/qodana-scan.yml@templates
          parameters:
            linter: 'jetbrains/qodana-js'
            maxMemory: '384m'
            condition: and(succeeded(), eq(variables['detect.skipHeavyCI'], 'false'))

        - template: steps/qodana-quality-gate.yml@templates
          parameters:
            failThreshold: 0
            failOnCritical: true
            condition: and(succeeded(), eq(variables['detect.skipHeavyCI'], 'false'))
